server:
  port: 7001
eureka:
  instance:
    hostname: eureka7001.com #eureka实例名字
  client:
    register-with-eureka: false #表示不会将自己注册进eureka
    fetch-registry: false #表示自己就是注册中心，职责是维护服务实例，并不需要去检索
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/